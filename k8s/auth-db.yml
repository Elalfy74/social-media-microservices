apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-db
  template:
    metadata:
      labels:
        app: auth-db
    spec:
      containers:
        - name: auth-db
          image: postgres:13
          env:
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "123"
            - name: POSTGRES_DB
              value: "auth"
---
apiVersion: v1
kind: Service
metadata:
  name: auth-db-svc
spec:
  selector:
    app: auth-db
  ports:
    - name: db
      protocol: TCP
      port: 5432
      targetPort: 5432
